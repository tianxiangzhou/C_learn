#include <stdio.h>
#include <stdlib.h>

enum day
{
    MON = 1,
    TUS,
    THR,
    WES,
    FRI,
    SAT,
    SUN
};

enum
{
    STATE_RUNNING = 1,
    STATE_CANCELED,
    STATE_OVER
};

// struct job_st
// {
//     int    id;
//     int    state;
//     time_t start, end;
// };


int main( )
{
    // enum day a = MON;


    exit(0);
}